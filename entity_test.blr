/* entity declarations */

entity e1 {
    clr = (255, 0, 255);
    size = (20, 20);
    bool dog = false;
    int time = 0;
    color my_clr = (0 ,0, 0);
    frame -> { 
        self.clr = self.my_clr;
        self.pos[0] = self.pos[0] + 10; 

        /* PARABOLA FOR ANGRY BIRD */
    	/* self.time = self.time +1;
        self.pos[1] = 300 - 30 * self.time + 1*self.time*self.time; */
    }
}

entity e2 {
    clr = (0, 0, 255);
    size = (20, 20);
    bool dog = false;
    int time = 10;
    color my_clr = (0,255,0);
    frame -> { 
        self.clr = self.my_clr;g
        self.size = (self.size[0]+1, self.size[1]+1);
        self.pos[1] = self.pos[1] + 10; 
        self.dog = true;
        self.time = 20;
    }
}


/* gameboard declaration */
gameboard g1{
	clr = (255,255,255);
	size = (680,420);
	init -> {
		int i = 0;
		i = i + 5;
		add(e2, (600, 20));
        add(e1, (20,300));
	}
}
